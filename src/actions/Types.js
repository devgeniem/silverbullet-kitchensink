// A list of all actions in the system.
import { createTypes } from 'reduxsauce';

export default createTypes(`
  CREATE_LIST,
  REMOVE_LIST,
  MODIFY_LIST,
  REFRESH_LIST,
  LOGIN_USER,
  REGISTER_USER,
`);
